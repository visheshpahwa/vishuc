#include<stdio.h>
#include<string.h>

int main()
{
    char s1[]="Vishu";
    char s2[]="Mukul";
    char s3[55];
    puts(strcat(s1,s2));
    // printf("The length of string 1 is %d\n",strlen(s1));
    // printf("The length of string 2 is %d\n",strlen(s2));
    // puts(strrev(s1));
    // puts(strcpy(s3,s1));
    // printf("%d",strcmp(s1,s3));
    return 0;
}